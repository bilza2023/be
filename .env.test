
# .env.test

# Test-specific DB (auto-created if missing)
MONGO_URI=mongodb://127.0.0.1:27017/taleemDB_test
ADMIN_SECRET=workshop-super-secret
# Token used only for tests
JWT_SECRET=test-secret

# Optional: Port not used in tests (no app.listen)
PORT=3001


